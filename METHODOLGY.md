## cancel adding text

    * Make an even listener on the body (aka the whole document) (T)
    * Make an even listener on the PdfViewer (aka the whole document) (TC)
    * Make an event listener on the canvas itself (F)
